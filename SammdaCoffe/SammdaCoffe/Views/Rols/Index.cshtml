@model IEnumerable<SammdaCoffe.Models.Rol>

@{
    ViewBag.Title = "Index";
}

<center>
    <h2>Roles</h2>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table" border="1" bgcolor=#F9EBEA>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.rolName)
            </th>
            <th>
                Acciones
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.rolName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.rolID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.rolID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.rolID })
                </td>
            </tr>
        }

    </table>
    <p>
        @Html.ActionLink("Regresar", "Menu", "Home")
    </p>
</center>
